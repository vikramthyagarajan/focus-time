

function Modal(props) {
  return <div>
  </div>
}

Modal.Header = function ModalHeader(props) {
  return <div>
  </div>
}

Modal.content = function ModalContent(params) {
  return <div>
  </div>
}

Modal.footer = function ModalFooter(params) {
  return <div>
  </div>
}

export default Modal